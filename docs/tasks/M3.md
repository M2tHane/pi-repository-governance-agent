# M3：Health Auditor → 手动／定时健康检查 → 报告与趋势

状态：M3 实现与验收完成｜前置实现：M2 技术基线｜更新：2026-09-12｜完成：9 / 9

## 授权与阶段边界

2026-09-12，用户要求“继续把当前项目往下一个阶段进行”，据此启动 M3。代码基线为 `3a454fd`（`feat: add M2 replies and selective multi-agent review`）。

M2 仍为 7/9；进入 M3 不代表维护者已确认多 Agent 的新增价值，不勾选 M2-06/09，也不迁移其 proposed Note。M2 的剩余验收及证据继续保留在 [M2 清单](./M2.md#质量判定与剩余门槛)。

用户随后明确要求“不要使用 supie 相关的技能，开始 M3 阶段”。据此直接实施已有 F06 范围，取消额外的原型／方案审批关卡。遵循项目文档分工：产品需求写入 PRD，执行状态与证据只写本清单，架构理由写 Notes。

需求主入口：[PRD-MVP · F06](../PRD-MVP.md#f06仓库健康检查与报告m3)。相关基线：[architecture](../architecture.md#84-healthreport)、[GitHub 权限](../github-integration.md#2-github-app-最小权限)、[可靠性与预算](../reliability-security.md)。

## 启动时技术基线（2026-09-12）

| 项目 | 当前事实与证据 |
| --- | --- |
| 运行时与构建 | `package.json` 要求 Node.js 24+；本机 `node --version` 为 v24.18.0、`npm --version` 为 11.16.0；TypeScript 7，ESM，现有 npm scripts。 |
| Agent | Pi SDK 0.85.1；`runAgentSession` 已提供只读会话、请求预留、usage 累计、超时与取消。 |
| 队列与持久化 | PostgreSQL / pg、一个 `PersistentRunner`；迁移目录当前至 `005_agent_usage.sql`。 |
| 管理界面 | React 19、现有 Node HTTP 管理 API、维护者 OAuth／CSRF。沿用当前风格。 |
| Workspace | 服务绑定 SHA 并创建／清理临时 checkout，读取校验路径与符号链接；不执行待审仓库脚本。 |
| 验证 | Node 内置测试；数据库测试使用一次性 schema；M0/M1/M2 的验证入口继续保留。 |

以下是启动时技术规划需要处理的边界，不代表当前仍未实现；对应完成证据见下文：

- `ReviewJob`、Job 表和执行路由具有 PR 语义。健康任务应成为明确的仓库级任务，不能用虚构 PR 编号兼容。
- 当前队列没有健康任务优先级；必须保证后台健康任务不淹没 PR 协作任务。
- 当前 Workspace 的 Git 调用缺少取消／时限参数；M3 的总时限必须覆盖准备与采集阶段，不能只包住模型调用。
- 当前 GitHub Client 尚无默认分支与 CI 采集入口；Actions／Checks／Security alerts 默认未申请，缺失数据必须可见。
- 仓库登记沿用现有管理页范围，首版不扩展 installation 仓库同步。
- Memory 沿用现有可召回版本语义和数量上限，报告必须列出实际提供的 id/version 与覆盖限制。
- 健康结果绑定固定快照；默认分支推进不把历史分析伪装为最新分支结果。趋势比较必须有可比性声明。

## 任务清单

- [x] M3-01：明确首版范围与实施边界。
- [x] M3-02：仓库级健康 Job、数据库迁移、去重与持久化。
- [x] M3-03：固定 SHA Workspace、受限文件清单与 CI 证据采集。
- [x] M3-04：独立 Health Auditor、结果校验与跨重试预算。
- [x] M3-05：手动／定时触发与重启恢复。
- [x] M3-06：权限、全流程取消／时限与 PR 领取优先级。
- [x] M3-07：Health 管理页、详情、历史对比与权限边界。
- [x] M3-08：本地／数据库／界面验证及 M0～M2 回归。
- [x] M3-09：固定样本与授权测试仓库的真实验收、文档收尾。

## M3-01 当前范围与验收

需求、优先级、默认值、异常状态和 H01～H10 的 done-when 均在 [F06](../PRD-MVP.md#f06仓库健康检查与报告m3)，本节只记录决定与实施状态。

首版建议：默认分支固定 SHA、最近 30 天窗口；手动触发；定时默认关闭，可选每日／每周；管理页持久报告和可比历史对照。保留 CI 缺失、覆盖不足和失败的真实状态。

已有事实：

- 产品与工程两个只读角色分别核对范围、可见性、外部权限和现有代码复用边界。
- 独立规格评审结论为 Approved，无阻塞问题；其建议的 PR／Health 规则限定已补入 AGENTS.md。
- 用户明确要求跳过相关技能并直接开始 M3，按上述首版范围实施。
- 实现顺序：任务／数据基础 → 快照与 Agent → 触发及权限 → 管理页 → 回归与真实只读验收。

## 验证计划与证据位置

| 类别 | 必须覆盖 |
| --- | --- |
| 输入与权限 | OAuth / CSRF、仓库归属、暂停／撤销、非法调度参数、空仓库。 |
| 快照与只读 | 默认分支解析、固定 SHA、路径／符号链接越界、仓库提示注入、禁止脚本执行。 |
| 调度与恢复 | 手动／定时活动任务去重、单 Worker 优先级、漏周期最多补一次、重启、有限重试。 |
| Agent 与报告 | 结构与行号／来源校验、实际 Memory 版本、缺失数据、预算、未知 usage、超时、取消、partial。 |
| 历史与界面 | 空／等待／失败／无权限状态、可比较性、零 findings 文案、移动端和键盘操作。 |
| 回归 | PR Review、Reply Handler、Decision Extractor、Memory 生命周期及现有发布语义。 |

实施阶段沿用实际命令：

```sh
npm run build
npm test
npm run test:db
npm run check:docs
npm audit --audit-level=high
```

真实验收继续限定 `M2tHane/pi-review-test-repository`。M3 只读检查无需在 GitHub 发布评论或修改源码；如后续样本需要新的外部写操作或权限变更，先明确具体动作。固定样本记录仓库／SHA、窗口、Job／报告 ID、模型、耗时、usage、数据缺失和限制，不能把本地替身当作真实采集证明。

## 执行记录

### 2026-09-12：阶段启动与需求收敛

- 实际操作：`git status --short`、`git log -4 --oneline`、Node/npm 版本检查、现有 package / migration / test 入口核对；通过 CodeGraph 检查只读 Workspace 与相关模块。
- 工作区起点：无已跟踪文件改动；`.codegraph/`、`.serena/` 为已有本地工具目录，保留。
- 当前产物：F06 需求草案、M3 任务清单、AGENTS.md 阶段入口更新。
- 独立评审：`m3_user_requirements`、`m3_dev_requirements` 分别给出需求与工程边界；`m3_spec_review` 结论为 Approved，无阻塞问题。
- 文档检查：`npm run check:docs` 通过，5 篇 Note 的结构／格式检查通过；一次性 Python 检查本轮三个文档中的 26 个相对链接与锚点、M3 的 0/9 状态和 M2 的 7/9 状态，全部通过；`git diff --check` 通过。首次链接检查发现 M3 的 GitHub 权限锚点不匹配，已修正并复验。
- 阶段启动时尚未运行构建、单元／数据库测试及依赖审计，当时只有需求文档；后续结果见最终验收。
- M2 人工增益确认：仍待回复，以 M2 清单为唯一验收记录。

### 2026-09-12：直接实施

- 依据用户的新指令开始实现，不再使用 Supie 相关技能或审批步骤。
- 使用现有 Node 测试设施先验证新边界；真实队列与数据库测试继续隔离。
- 实现和最终验证结果完成后在此补充证据，尚未运行的步骤不标为通过。

### 2026-09-12：实现与阶段验证

- `006_health_audit.sql` 增加独立 HEALTH_AUDIT、活动任务唯一约束和按 Job 唯一的报告；`007_installation_revocation.sql` 允许 installation 级 delivery 不绑定虚构仓库。
- 新增回归先观察到缺失功能失败，再验证手动／定时去重、PR 优先、固定 SHA、跨重试预算、请求前持久化、报告与终态一致性、暂停和授权撤销。
- 阶段性 `npm test`：52 项，41 通过、11 项数据库检查跳过；`npm run test:db`：12 项全部通过；`npm audit --audit-level=high`：0 漏洞。随后增加的授权撤销与输入边界检查分别通过，最终完整回归待汇总。
- React Health 页面接入现有 OAuth、仓库设置、报告详情与历史对比；TypeScript 构建现在也检查 `admin/**/*.tsx`。
- 使用 ego-browser 的同一任务空间 1 验证真实管理页。一次 OAuth 网络失败后，连接检查通过，重新登录恢复。桌面 1440px 与移动端 390px 的页面宽度均未溢出，截图见 `work/m3-health-desktop.png`、`work/m3-health-mobile.png`。
- 真实手动 Job `d9c3124c-2099-48b0-9df7-99e043e77da6`：默认分支 `main`，SHA `3e02a7c59b073547da3c466987d4d8570df31484`；角色 `health_auditor`，模型 `deepseek/deepseek-v4-flash`，24,397 ms，8,438 tokens（input 3,059 / output 3,459 / cacheRead 1,920 / unknown 0）。9 个选定文件均返回全部行，形成 4 条结构与来源已校验的意见；未把这些意见全部视为人工确认问题。由于 Checks / Actions 没有记录，报告为 partial。
- 真实结果：[Health 报告](https://texas-pot-venues-whenever.trycloudflare.com/#health/d9c3124c-2099-48b0-9df7-99e043e77da6)。没有创建 GitHub 评论、Issue 或源码提交。

## 最终验收

2026-09-12，服务基于 `3a454fd` 的工作区改动完成 M3；本轮没有创建新的代码提交。实现取舍见 [Health Auditor Note](../../.agents/notes/implemented/architecture/2026-09-12-m3-health-auditor.md)。

| 实际命令／操作 | 结果 |
| --- | --- |
| `npm run build` | 通过；包含服务、测试和 React TSX 的 TypeScript 检查及管理页构建。 |
| `npm test` | 53 项：41 通过、12 项数据库检查由独立命令运行；0 失败。 |
| `npm run test:db` | 13 项全部通过；每次创建并回收独立 schema，未领取真实任务。 |
| `npm run check:docs` | 6 篇 Note 的结构、格式及内部链接检查通过。 |
| `npm audit --audit-level=high` | 0 漏洞。 |
| 相对路径／锚点与 `git diff --check` | 通过；本轮修改文件未检测到本地真实凭据值。 |
| ego-browser 真实界面 | OAuth 登录、手动运行、状态更新、历史切换、Jobs → Health 链接和键盘展开通过。1440px / 390px 视口均无页面横向溢出。 |

回归覆盖：仓库级 Job 无伪造 PR 字段、活动任务唯一、首次报告与终态一致、PR 优先领取、未授权仓库不占用定时扫描窗口、UTC 周期去重、漏周期合并、原快照重试、预算预留先于 Provider 请求、未知消耗恢复、文件与源码行范围、CI 身份／SHA／窗口校验、OAuth／CSRF、App 撤销的签名／归属／幂等、取消状态不被迟到回写复活，以及 M0～M2 既有流程。

### 两次真实只读检查

仓库均为 `M2tHane/pi-review-test-repository`，默认分支 `main`，固定 SHA 均为 `3e02a7c59b073547da3c466987d4d8570df31484`；角色 `health_auditor`，模型 `deepseek/deepseek-v4-flash`，每任务预算 160,000 tokens。

| Job / 报告 | 固定窗口（UTC） | 耗时 | usage | 结果 |
| --- | --- | --- | --- | --- |
| [d9c3124c-2099-48b0-9df7-99e043e77da6](https://texas-pot-venues-whenever.trycloudflare.com/#health/d9c3124c-2099-48b0-9df7-99e043e77da6) | 2026-08-13 07:47:57.297 → 2026-09-12 07:47:57.297 | 24,397 ms | input 3,059 / output 3,459 / cacheRead 1,920 / total 8,438 / unknown 0 | partial；9 个文件全部行已返回，4 条带来源的意见。 |
| [d54dbfba-5242-461a-b550-1ea1f6920a24](https://texas-pot-venues-whenever.trycloudflare.com/#health/d54dbfba-5242-461a-b550-1ea1f6920a24) | 2026-08-13 09:02:08.671 → 2026-09-12 09:02:08.671 | 19,811 ms | input 2,807 / output 2,496 / cacheRead 2,176 / total 7,479 / unknown 0 | partial；9 个文件全部行已返回，2 条带来源的意见。 |

两次检查都没有可用的 Checks / Actions 记录，CI 维度明确不可比；代码、依赖、文档维度显示同口径观察数量和严重度分布。相同源码出现 4 → 2 条模型意见，说明数量波动不能作为修复证明，界面也没有自动生成“已修复”判断。

原 ACTIVE Memory `df080917-5a9d-4059-9b3c-e0be1fdd0dd8` v2 作为实际输入规则记录在报告中，Health 没有修改其状态或版本。结构化证据保存在受 Git 忽略的 `work/m3-acceptance.json`；最终截图为 `work/m3-health-desktop.png`、`work/m3-health-comparison.png`、`work/m3-health-mobile.png`。

### 边界与交接

- 定时配置最终为 `off`，next run 为空；界面验证了频率选择与关闭保存，日／周到期、重复与停机恢复使用固定时钟的数据库测试验证，没有等待真实自然日。
- App 删除／移除的取消通过验签 HTTP 与临时数据库验证，没有实际卸载测试 App 或改变其权限。真实 CI 缺失是报告的明确限制，正常／异常 CI 元数据由替身覆盖。
- 联调期间出现过 OAuth 网络失败及服务进程退出后的 502；恢复连接与服务后，重新完成了历史报告切换和 Jobs 链接验证。报告已持久保存，刷新和重启没有丢失。
- M2 仍保持独立的 7/9 人工增益验收状态，M3 完成不替代该确认。
